model: WSSNet-Tiny
num_classes: 2
input_shape: [256, 256]
preprocessing:
  color: RGB
  resize: letterbox
  normalization: divide_by_255
  postprocess: softmax_argmax
training:
  epochs: 100
  batch_size: 8
  lr: 0.0001
  num_workers: 2
  seed: 11
  weight_decay: 0.0
